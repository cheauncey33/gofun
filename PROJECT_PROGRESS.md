# WHU_Snack_GO 项目实战与大厂面经指南 (Project Progress & Interview Guide)

## 🎯 核心目标 (Project Goals)
- **大厂实习导向**：摒弃单纯的增删改查 (CRUD)。通过引入 Redis 和 RabbitMQ，打造具备“高并发秒杀”能力的电商系统，拉升简历技术含金量。
- **技术深度与八股融合**：在项目开发中穿插底层原理讲解，做到“知其然，更知其所以然”，无缝应对面试官的深挖。
- **自主学习为主**：Antigravity (AI 导师) 绝不包办代码。采用“**导师引导 -> 碎片化提供知识点/代码块 -> 开发者手动敲写 -> 深入原理答疑**”的学习闭环进行日常开发。

## 🤖 “导师制”互动约定 (Mentor & Student Rules)
- **拒绝一键生成**：我不会一次性丢给你成百上千行的代码。每次我们只聚焦一个小模块。
- **实敲辅导模式**：你负责参考我提供的逻辑和代码片段，在你的本地编辑器里亲自敲出来。遇到看不懂的 Go 语法、框架机制，第一时间向我提问。
- **面试级灵魂拷问**：在咱们敲完一个模块后，我会主动跳出来扮演“面试官”，提出相关的八股文问题，引导你思考，并在讨论后将精华总结进此文档。

## 🛠 技术栈演进路线 (Tech Stack Roadmap)
*(当前开发环境基于本地直接安装，不涉足 Docker，专注纯粹的后端组件)*

- **第一阶段（基础闭环）**：Go + Gin + Vue3 + MySQL + GORM + JWT。*(目前正处于收尾阶段)*
- **第二阶段（高并发与缓存）**：引入 **Redis**。解决业务痛点：首页热点商品缓存、库存预扣减、分布式锁防超卖。
- **第三阶段（异步与并发削峰）**：引入 **RabbitMQ**。轻量易上手。解决业务痛点：秒杀高并发大流量削峰、订单异步落库。

## 📈 当前项目真实进度 (Current Progress)
> 更新日期：2026-04-02 | 同步最新代码状态

### 1. 基础架构层
- [x] **本地环境**：MySQL, Redis 本地服务处于 Ready 状态。
- [x] **前后端框架搭建**：Gin 路由分组，Vue 视图切换搭建完毕。

### 2. 鉴权与用户模块
- [x] **JWT 核心逻辑**：`backend/common/jwt.go` Token 生成与工具类写完。
- [x] **鉴权中间件**：Gin 中间件拦截并验证 Token 已搭建。
- [x] **前端鉴权流**：`Login.vue` 界面与 Axios 拦截器写入 Header 逻辑打通。
- [x] **用户注册/登录后端接口完善**：实现与 MySQL 数据库的真实账号校验。

### 3. 商品与基础订单模块
- [x] **前端商品展示页面**：`Product.vue` 界面设计完毕，包含表格、购买弹窗、分页切换逻辑。
- [x] **商品列表后端 API**：包含分页参数解析和 GORM 数据库查询。
- [x] **普通下单流程修复**：当前前端写死了 `user_id: 1`，后端需获取 JWT 上下文中的真实用户 ID，实现安全的下单权限控制。

### 4. 🚀 进阶：高并发秒杀模块 (待开发)
- [ ] **库存预热**：商品库存同步至 Redis。
- [ ] **防超卖机制**：基于 Redis (Lua 脚本或分布式锁) 的库存原子扣减。
- [ ] **异步落库**：RabbitMQ 发布秒杀成功消息，异步创建完整订单数据。

---

## 🎒 大厂面经与八股充电站 (Interview Q&A)
> 此板块由导师 (Antigravity) 负责维护。随着我们敲代码的进度，这里将不断沉淀我们在对话中探讨过的重难点面试题，保证有问有答，直接复刻背诵。

### 📌 专题一：基础架构与网络 (Go/Web)
- **Q1: 为什么在项目初始化遇到连接失败时，选择 `panic` 而不是返回 error？**
  - **A**: 这是典型的**快速失败 (Fail Fast)**。与其带病上岗导致雪崩，不如直接崩溃，由容器兜底重启。
- **Q2: 在 Go 语言打印或返回错误时，`fmt.Errorf` 里的 `%v` 和 `%w` 有什么区别？**
  - **A**: `%v` 会将错误强行转换为普通的文本字符串，一旦转换，原始错误的“类型信息”就彻底丢失了。而 `%w` 是 Go 1.13 引入的**错误包装 (Error Wrapping)**。它像包洋葱一样把底层错误包在里面，上层调用者依然可以使用 `errors.Is(err, gorm.ErrRecordNotFound)` 来判断底层到底发生了什么根因。大厂规范中要求向下传递内部错误时，必须使用 `%w`。

### 📌 专题二：鉴权与安全 (JWT 篇)
- *(待整理)*

### 📌 专题三：高并发缓存 (Redis 篇)
- **Q1: Redis 既然是键值对存储，代码里的 `DB: 0` 是什么意思？**
  - **A**: 最佳实践是只用 DB 0 并物理隔离。同项目内通过冒号前缀 (如 `snack:stock:1`) 来作为命名空间区分。
- **Q2: 秒杀系统的大流量防超卖拦截点（如 Redis Decr 扣减），应该放在 MySQL 事务的里面还是外面？**
  - **A**: **必须放在最外面（甚至比分配数据库连接更早）！**。如果放在事务内部（即便在行锁前面），几万个请求依然会瞬间建立几万个 MySQL Connection 从而导致连接池枯竭。我们的目标是“在它们进入客厅前就把门关上”，绝不能引入任何对 DB 侧的压力。
- **Q3: 为什么 Redis 的代码写起来这么简单（就是 Get/Set/Decr），但简历上写了 Redis 就会面临巨大的面试压力？（架构师灵魂追问）**
  - **A**: 因为写 API 调用极其简单，但把它放入生产环境会诱发极端复杂的衍生问题。主要考察三点：
    1. **常见灾难防御**：如果你代码里的 `redisKey` 写错了，或者遭到黑客狂刷不存在的商品 ID，请求全打进 MySQL 怎么办？（缓存穿透）；如果所有库存由于加了同样的过期时间，同一秒同时过期，下一秒十万流量击垮 DB 怎么办？（缓存雪崩）。
    2. **数据双写一致性**：我们用 Redis 做的预扣减成功了，但随后 Go 的老代码写 MySQL 订单因为别的原因（如积分不足）发生报错回滚了。这时 Redis 的缓存变成了 -1，MySQL 啥事没有，导致数据永远不同步怎么办？（延时双删、使用 MQ 异步补偿重试）。
    3. **集群高可用**：这台运行 Redis 的电脑拔电源宕机了，内存数据瞬间没了怎么恢复？（AOF/RDB 机制、哨兵 Sentinel 或 Cluster 集群）。

### 📌 专题四：消息队列与削峰 (RabbitMQ 篇)
- *(待开启)*
