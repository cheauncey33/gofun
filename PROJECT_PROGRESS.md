# WHU_Snack_GO 项目进度记录与技术储备 (Project Progress & Technical Notes)

## 🎯 核心目标 (Project Goals)
- **大厂实习导向**：摒弃单纯的增删改查 (CRUD)。通过引入 Redis 和 RabbitMQ，打造具备“高并发秒杀”能力的电商系统，拉升简历技术含金量。
- **技术深度与八股融合**：在项目开发中穿插底层原理讲解，做到“知其然，更知其所以然”，无缝应对面试官的深挖。
- **自主学习为主**：Antigravity (AI 导师) 绝不包办代码。采用“**导师引导 -> 碎片化提供知识点/代码块 -> 开发者手动敲写 -> 深入原理答疑**”的学习闭环进行日常开发。

## 🤖 “导师制”互动约定 (Mentor & Student Rules)
- **拒绝一键生成**：我不会一次性丢给你成百上千行的代码。每次我们只聚焦一个小模块。
- **实敲辅导模式**：你负责参考我提供的逻辑和代码片段，在你的本地编辑器里亲自敲出来。遇到看不懂的 Go 语法、框架机制，第一时间向我提问。
- **面试级灵魂拷问**：在咱们敲完一个模块后，我会主动跳出来扮演“面试官”，提出相关的八股文问题，引导你思考，并在讨论后将精华总结进此文档。

## 🛠 技术栈演进路线
*(当前开发环境基于本地环境运行，暂未使用容器化方案)*

- **第一阶段（基础闭环）**：Go + Gin + Vue3 + MySQL + GORM + JWT。*(目前正处于收尾阶段)*
- **第二阶段（高并发与缓存）**：引入 **Redis**。解决业务痛点：首页热点商品缓存、库存预扣减、分布式锁防超卖。
- **第三阶段（异步与并发削峰）**：引入 **RabbitMQ**。轻量易上手。解决业务痛点：秒杀高并发大流量削峰、订单异步落库。

## 📈 当前项目进度
> 更新日期：2026-04-03 | 同步最新代码状态

### 1. 基础架构层
- [x] **本地环境**：MySQL, Redis 本地服务就绪。
- [x] **前后端框架搭建**：Gin 路由分组，Vue 视图与路由配置完成。

### 2. 鉴权与用户模块
- [x] **JWT 核心逻辑**：`backend/common/jwt.go` Token 签发与解析功能完成。
- [x] **鉴权中间件**：Gin 路由请求鉴权拦截。
- [x] **前端鉴权流**：`Login.vue` 与 Axios 请求拦截器注入 Header (`Authorization`)。
- [x] **用户注册/登录后端接口完善**：实现 MySQL 真实的账号状态及密码摘要 (Bcrypt) 校验。

### 3. 商品与基础订单模块
- [x] **前端商品展示页面**：`Product.vue` 表格视图、下单弹窗、分页组件功能完成。
- [x] **商品列表后端 API**：实现分页参数解析与后端商品查询。
- [x] **后端权限流转修复**：移除前端强制传入 `user_id` 的越权隐患，通过 JWT Context 提取真实用户 ID 并实现下单限制。
- [x] **前端 JWT 兜底流转修复**：引入全局 Response Interceptor，拦截 401 未授权响应，强制页面返回登录态闭环。

### 4. 🚀 进阶：高并发秒杀模块 (待开发/开发中)
- [x] **缓存预热**：商品上架及项目启动时，将 MySQL 剩余库存全量同步至 Redis。
- [x] **流量拦截与防超卖**：基于 Redis 内存机制的原子扣减 (`DecrBy`) 及业务失败后的缓存回滚。
- [x] **MQ环境构建**：使用 Docker 启动 RabbitMQ 并完成 Go 服务端的全局连接池注册。
- [x] **异步落库重构**：将同步的 MySQL CRUD 分离，实现前台 Producer (发布者) 极速发信 与 后台 Consumer (消费者) 异步落库的数据处理解耦。

### 5. 🛡 进阶：大厂级高可用与容错 (开发中)
- [x] **接口幂等性 (防重)**：引入雪花算法生成唯一请求ID与数据库唯一键防冲突，解决 MQ 重复投递导致的数据错乱。
- [x] **系统限流 (Rate Limiting)**：在 Gin 网关层引入令牌桶限流，防止爬虫/黄牛利用请求轰炸系统，彻底阻断无效超额流量。
- [x] **优雅启停 (Graceful Shutdown)**：介入操作系统中断信号，保证服务在重启/退出时，所有的 MQ Consumer 能够安全收尾处理，防止脏数据的产生。
- [x] **热点数据多级缓存**：引入本地缓存 (Go-Cache) + Redis 二级缓存，应对万级 QPS 下对 Redis 的热点 Key 访问压力。
- [x] **最终一致性补偿**：建立定时任务核对 Redis 扣减逻辑与数据库实际订单数，自动回补异常丢切的消息，解决分布式系统少卖问题。
- [ ] **订单状态异步轮询**：前端实现通过 WebSocket 或长轮询查询订单真实落库状态，优化高并发下的异步交互体验。
- [ ] **可观测性建设 (Logging/Metrics)**：集成结构化日志 (Zap) 与 Prometheus 监控，实时观察 QPS、错误率及核心链路耗时。
---

## 🎒 技术问答与面试储备
> 本板块总结项目中应用的核心技术点及其背后的架构设计原理。

### 📌 专题一：基础架构与并发控制 (Go/Web)
- **Q1: 为什么在项目初始化 (InitDB/InitRedis) 遇到异常时选择 `panic` 而非返回 error 继续运行？**
  - **A**: 采用**快速失败 (Fail Fast)** 原则。数据库或缓存是核心强依赖，带病启动会导致运行时的连锁故障或缓存击穿。初始化阶段阻断进程能够交由服务发现或容器管理工具进行重启自愈。
- **Q2: 在 Go 语言中处理内部错误上抛时，`fmt.Errorf` 的 `%v` 和 `%w` 有何区别？**
  - **A**: `%v` 会将错误转换为普通字符串，导致原始错误类型丢失。而 `%w` (Go 1.13 引入的 Error Wrapping) 能将底层错误对象封装于上层错误中，使得上层调用逻辑可以通过 `errors.Is()` 或 `errors.As()` 对根因进行精确的类型断言。
- **Q3: 为什么使用 `go-redis` 库操作计数值时，参数强制要求使用 `int64`？**
  - **A**: Go 是强类型语言，不存在隐式转换。Redis 内部处理整数采用 64 位有符号整型标准，因此驱动层 API 显式要求 `int64`，此设计避免了运行时的溢出风险与类型异常。

### 📌 专题二：访问控制与安全 (JWT 与限流)
- **Q1: 后端 `GetUserInfo` 与下单接口如何安全地识别当前请求的具体用户身份？**
  - **A**: JWT 在签发阶段 (`GenerateToken`) 会将用户 ID 等非敏感关键信息附在 Payload 中并通过 `MySecret` 签名。客户端随后携带该 Token 请求。由于签名机制保证防篡改，后端可信任 Token 中解析出的声明 (Claims)。
- **Q2: 业务系统如何透传中间件解析出的 `user_id`？**
  - **A**: 通过 Gin 框架的 **Context (上下文)**。Auth 中间件验证 Token 有效后，执行 `c.Set("user_id", claims.UserID)`。在随后的 Controller 或 Service 执行阶段，通过 `c.Get("user_id")` 读取，实行业务解耦。
- **Q3: 若商品列表查询 (GET /products) 不受 JWT 限制约束，系统应如何防御恶意的高频脚本请求访问不存在的资源？**
  - **A**: 对该类公开且高频的读接口，应在应用网关或中间件层引入 **API 限流 (Rate Limiting)** 控制访问频次，并结合 **布隆过滤器 (Bloom Filter)** 等机制快速过滤非法的查询标识码。
- **Q4: 为什么我的业务 Controller 没有写任何抛出 401 (`Unauthorized`) 的代码，但由于 Token 过期依然被拦死返回了该状态码？**
  - **A**: 这是基于 Gin 框架等主流 Web 体系的 **Middleware (中间件侵入式拦截)** 原理。由于在注册 Router 组时使用了 `auth.Use()` 加载鉴权中心，中间件拥有切面操作的能力（AOP 面向切面编程）。若校验阶段失败，它会通过 `c.AbortWithStatusJSON(401)` 主动阻断后续整个请求链路的执行。也就是说请求根本还没摸到 Controller 的门，就已经被保安踢出了服务器。
- **Q5: 为什么选择令牌桶 (Token Bucket) 算法而非漏桶 (Leaky Bucket)？**
  - **A**: 漏桶专注于“平滑”输出流量，强制请求排队逐个处理，无法应对秒杀瞬时的爆发流量。而**令牌桶**允许在桶内有令牌时进行一定程度的**突发(Burst)**处理，更适合电商“整点秒杀”瞬间涌入数千个合法请求的真实业务需求。
- **Q6: 在代码中实现 IP 限流时，Map 锁竞争和内存泄露如何考虑？**
  - **A**: 采用 `RWMutex` 读写锁分离，极大减少了高并发下的只读检查开销。对于 IP 生成的 Limiter 对象，由于秒杀通常持续时间极短，暂存在内存 Map 中是成本最低的方案。在更严苛的生产环境中，通常会引入 Redis 结合 Lua 脚本实现分布式的滑动窗口限流或令牌桶，从而支持集群扩容并解决单机内存回收问题。

### 📌 专题三：高并发缓存策略 (Redis)
- **Q1: Redis 的 `DB: 0` 以及类似 `snack:stock:1` 命名规范的意义是什么？**
  - **A**: Redis 默认开启 16 个逻辑库，生产实践中建议使用默认 DB 0 实现同实例资源收敛。逻辑层面的业务区分依赖**冒号前缀分隔符** (e.g., `namespace:module:id`)。这种约定的命令空间机制可被绝大多数可视化工具识别为树状路径，便于分类管理。
- **Q2: 高并发场景下的预扣减拦截操作，应该防止在数据库事务的内侧还是外侧？**
  - **A**: **必须置于数据库事务开启之前**。即使置于具体行锁（`FOR UPDATE`）之前，只要进入 `DB.Transaction` 就已占用数据库连接池资源。如果缓存前置无法拦截无效流量，极度并发的请求依然会造成连接池枯竭。
- **Q3: Redis 单线程模型的效率为何能满足极高并发的设计要求？**
  - **A**: Redis 是基于纯内存操作的数据结构存储服务器，操作耗时位于纳秒级别，且单线程架构规避了多线程的上下文切换与锁竞争开销。基于 IO 多路复用，一台标准机器的单核 QPS 可突破十万次。就算是内存的单线程也比硬盘的mysql快。
- **Q4: Redis `DecrBy` 扣减不足时为何会返回负数而非常规错误？业务代码需做何处理？**
  - **A**: Redis 内部为保证指令原子性，盲目执行递减计算。当初始库存为 0 时，结果会变为 `-1` 并写入内存。Go 代码探测到返回值 `< 0` 说明存在超卖可能，应当阻断交易流程并利用反向补偿操作也就是说执行对应的 `IncrBy` 对由于扣减执行导致的不一致负数数值进行调平处理。
- **Q5: 解释缓存穿透 (Cache Penetration) 的形成及工程规范解法。**
  - **A**: 当请求访问系统内根本不存在的数据时，由于 Redis 未命中，请求持续落到 MySQL 等后置数据库，严重浪费资源乃至压垮数据库。方案以两点为主：1. 对空数据建立短有效期的缓存 (缓存空对象)。2. 部署布隆过滤器 (Bloom Filter) 实现前置阻断。
- **Q6: 解释缓存雪崩 (Cache Avalanche) 的形成及工程规范解法。**
  - **A**: 指海量原本常驻于缓存层的热点数据，在极为接近的时间点由于 TTL 过期失效而同时涌向回源查数据库，导致负载过高。常见防范手段是在合理基础过期时间之上，对失效时刻进行一个微弱的随机打散扰动。
- **Q7: 如果经过了 Redis 初步扣减的订单请求在数据库入库及事务逻辑中发生了内部异常并需回滚，系统应如何应对此时产生的数据偏差？**
  - **A**: 典型的数据一致性场景 (双写一致纠偏)。服务编排层（Service 核心逻辑）可在捕捉到异常后并返回给上游 Controller 前对已经发生变更的 Redis 库存部分执行主动执行**回补补偿**。更复杂或无法预期的宕机跨系统断连类业务场景则需要考虑使用基于本地消息表任务重试补齐或是基于 MQ 架构的死信系统队列来最终保障业务数据状态的可靠一致性。

### 📌 专题四：消息队列与削峰解耦 (RabbitMQ 篇 - 概念原理)
- **Q1: 在有了前面“防弹盾”级别的 Redis 扣减挡住了绝大部分流量之后，最终放行的那几十个合法流量为什么还要扔进 RabbitMQ，而不是顺理成章地直接去写 MySQL？**
  - **A**: 这体现了架构设计的“极力解耦”与“削峰填谷”。哪怕只放行少数合法请求，直接交由 Go 同步开启数据库事务依然属于“重操作”（处理行排他锁、关联表创建更新）。如果有阻塞，导致前端 HTTP 产生漫长等待甚至超时（转圈卡死）。引入 MQ 相当于将业务流变为了**异步操作 (Asynchronous)**。业务主线在通过 Redis 后，只需耗费微秒级向 MQ 投递一句微小的 JSON 报文“某人抢到了某产品”即可立刻通过 HTTP 响应给前端购买成功。而那些沉重的 DB 写入作业，则完全交给后方默默工作的异步协程排队消化完毕。
  数据库连接池保护：即使有 Redis 挡住了 90% 的请求，剩下的 10% 合法流量在瞬时涌入时，如果每个请求都开启 MySQL 事务，会迅速耗尽数据库连接池。一旦连接池干了，整个服务（包括商品查询等）都会停摆。
  吞吐量（Throughput）提升：同步写库时，用户的请求耗时 = Redis扣减 + MySQL事务锁等待 + 网络IO。引入 MQ 后，耗时 = Redis扣减 + MQ发信（内存级 IO）。响应速度提升了百倍，系统每秒能承载的并发量自然水涨船高。
  隔离与降级：如果 MySQL 突然抖动或者宕机，只要 MQ 还在，订单请求就能“存”在队列里，不会直接报错，等数据库恢复后，后台消费者再慢慢消化（即“削峰填谷”）
- **Q2: 把写数据的（CRUD）剥离出去后，谁来执行这部分代码？MQ 能够直接修改数据库吗？**
  - **A**: 不能。MQ 的本质仅仅是一个高级的、具有持久化和路由能力的“网络缓存信箱”。真正去执行这些 CRUD 代码的，是我们在后端程序运行时单独开启的**消费者 (Consumer) 协程**。它游明白 Controller 请求生命周期之外，是个死循环任务。它的唯一使命就是监听信箱提取电报，然后慢条斯理地与 MySQL 建立连接并完整执行被剥离出的事务写操作。前者（发信）被称为 **Producer**，后者（拆信并写库）被称为 **Consumer**。
