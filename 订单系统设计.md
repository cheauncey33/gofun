# 订单系统设计说明

## 整体流程

```
用户下单
  → Redis Lua 脚本原子预扣库存（挡住超卖）
  → 生成 Snowflake 订单 ID，消息发进 RabbitMQ
  → 立即返回"排队中"（异步，用户不等 DB）

MQ 消费者（order_consumer.go）
  → 校验商品状态、扣 MySQL 库存、建 Pending 订单
  → 发一条 15 分钟延时超时消息

用户点支付（/orders/:id/pay）
  → 条件更新 WHERE status=pending → paid（挡并发）
  → 同一事务扣余额，余额不足整体回滚

超时未支付
  → 延时队列 TTL 到期 → 死信路由到超时队列
  → 消费者条件取消（WHERE status=pending）+ 回补库存
```

## 为什么这样设计

**异步下单**：把 DB 写操作移出请求链路，用 Redis 预扣挡超卖，MQ 削峰。秒杀场景下大并发不会打穿数据库。

**支付时才扣款**：下单只占库存，支付才动钱。标准电商做法，取消逻辑干净——未付款的取消只退库存，已付款的才退钱，由 `OrderStatus.HasBeenPaid()` 统一判断，不用在每处 if-else。

**延时队列超时**：用 RabbitMQ 的 `x-message-ttl` + 死信交换机（DLX）实现，消息在 delay 队列躺 15 分钟没人消费就自动变死信，路由到超时队列触发取消。比定时轮询全表的方案性能好很多——订单量大时扫表是灾难，延时队列只在真正超时时触发，零轮询开销。

## 订单状态机

```
Pending(1) ──支付──→ Paid(2) ──发货──→ Delivering(3) ──送达──→ Delivered(4)
    │                  │            │                              │
    └──取消──→         └──取消──→   └──取消──→                    └──申请退款──→
              Cancelled(5)                                         Refunding(6) ──→ Refunded(7)
```

核心：`CanTransitionTo()` 查邻接表，非法跳转直接拒绝，不用散落 if-else。

退款条件：`HasBeenPaid()` 返回 true 的状态（Paid/Delivering/Delivered/Refunding/Refunded）取消才退钱；Pending/Cancelled 取消只退库存。

## 并发安全：条件更新模式

所有改状态+动钱的操作都用同一个事务 + `WHERE status=期望状态`：

```sql
-- 支付
UPDATE `order` SET status=paid WHERE id=? AND status=pending
UPDATE user SET balance=balance-? WHERE id=? AND balance>=?

-- 超时取消
UPDATE `order` SET status=cancelled WHERE id=? AND status=pending
UPDATE product SET stock=stock+? WHERE id=?
```

`RowsAffected == 0` 说明已被并发操作抢先，直接返回错误，不会重复扣款或重复退款。

## Redis 库存与 MySQL 的一致性

- **强一致**：库存扣减走 MySQL 事务（`stock >= ?` 条件更新）。
- **Redis 缓存**：`snack:stock:<id>` 是前端展示用的近实时缓存，Redis 回补放在**事务提交之后**，避免事务回滚时 Redis 多加。
- **兜底**：`CompensationService` 每 5 分钟扫全量商品，发现 Redis > MySQL（超卖风险）就重置为 MySQL 值，做最终一致兜底。

## 重试与死信队列

消费者失败分两类：

- **不可重试**（余额不足、商品不存在等业务错误）：直接 Ack + 回滚 Redis 预扣库存，不进重试。
- **可重试**（网络抖动、DB 超时等临时错误）：带 `x-retry-count` 头重投到 `.retry` 队列，超过 `MaxRetries`（默认 3）后进死信队列（`.dlq`）人工处理。

## 幂等

下单接口支持 `idempotency_key`（客户端生成，≤128 字符）。`SetNX` 写 Redis，10 分钟 TTL。下单失败（同步失败或消费端不可重试失败）自动释放，用户可立即用同一 key 重试。成功的订单保留 key 防重复提交。

## 秒杀额外机制

1. Admin 预热：`seckill:stock:<id>` 写入 Redis，设 TTL 到活动结束时间。
2. 用户先请求 token：HMAC 签名，存 Redis 60 秒。
3. 执行秒杀：Lua 脚本原子验证 token + 检查库存 + 检查限购 + 扣库存，成功后走同一套 MQ 下单流程。
4. 活动结束后 `clearSeckillRedisKeys` 清理所有相关 Redis key。
