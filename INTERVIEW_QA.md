# WHU Snack - 秋招后端面经总结与沉淀

## 1. 高并发限流：漏桶算法 (Leaky Bucket) vs 令牌桶算法 (Token Bucket)

在设计高并发系统（如秒杀、抢购）时，网关层的限流是保护系统不崩溃的最重要防线之一。面试中常被问及这两种算法的区别及应用场景：

### 1.1 漏桶算法 (Leaky Bucket)
- **原理**：想象一个底部有孔的桶，水（请求）以任意速度流入桶中，但底部漏水的速度是**绝对固定匀速**的。如果流入速度过快导致桶满了，新来的水（请求）就会溢出（被丢弃或排队等待）。
- **特点**：
  - **强行平滑输出**：不管输入流量多么不均匀，输出流量一定是绝对平滑的。
  - **无法应对突发流量**：如果系统瞬间涌入大量请求，漏桶只能慢条斯理地按照固定速度处理，多余的请求必须等待或被直接丢弃，即使系统此刻其实有盈余的处理能力。
- **最佳场景**：常用于保护极度脆弱的、绝对不能承受任何波动的下游服务（如某些老旧的第三方支付接口，或者数据库写入任务）。

### 1.2 令牌桶算法 (Token Bucket) - 本项目采用
- **原理**：系统按照**恒定的速率 (`r`)** 将“令牌”放入到一个容量为 `b` 的桶里。如果有请求过来，必须先从桶中拿走 1 个令牌才可以去执行核心业务逻辑；如果桶里没有令牌，请求就会被拒绝（例如返回 429 Too Many Requests）。
- **特点**：
  - **允许一定程度的突发流量（重要！）**：这是令牌桶完胜漏桶的最大优势。当系统空闲时，桶里会最多攒下 `b` 个令牌（即 `Burst` 峰值容量）。如果此时瞬间涌入 `b` 个请求，这 `b` 个请求会**瞬间全部放行**拿到牌子，不会被强行限制为匀速通过。
  - 系统对波峰更加包容，能在“最高承载能力”和“平滑限流”中找到更好的平衡。
- **Go 语言实现细节**：
  - Go 标准库 `golang.org/x/time/rate` 底层正是令牌桶的实现。
  - **性能优化点**：Go 底层并未真正启动一个后台定时器（Goroutine）去每秒不停地投放令牌（那样极其损耗性能）。而是使用了**“懒汉式（惰性计算）”**的方式，只记录了“上一次更新的时间点”。当请求需要 `Allow()` 校验时，利用 `(当前时间 - 记录的上次时间点) * 生成速率 r` 这个极其轻量的数学公式，瞬间算出现在应得的令牌数，完成了高并发下的极致性能优化。

### 1.3 `map`并发安全与双重检查锁（DCL）
- 在实现按 IP 独立限流的 `IPRateLimiter` 时，由于需要维护一个 `map[string]*rate.Limiter` 的路由表，而 Go 的原生 `map` 并非并发安全（Concurrent writes 会导致 `panic`宕机），因此必须采用 `sync.RWMutex` 进行读写控制。
- 只有在读取不到当前 IP 限流器试图往 `map` 写入新数据时，才加 `Lock`（互斥写锁）。同时，拿到写锁进入临界区后，必须进行**二次探查 (`if !ok`)**。这防止了多个 Goroutine 在排队等锁时，排在前面的协程已经创建好了实例，而自己又错误覆盖重叠的常见并发缺陷（双重检查锁模式）。

## 2. 优雅启停 (Graceful Shutdown)

在生产环境中，服务的重启、发版、扩缩容是家常便饭。如果直接暴力杀进程，正在处理中的请求和消息会丢失，造成脏数据。

### 2.1 核心思路：分层有序撤退
优雅启停本质上是一个**"分层倒序关闭"**的过程，需要区分不同组件的关闭策略：
1. **第一层：业务消费者（MQ Consumer）**：通过 `context.WithCancel` 创建的 Context 传递"下班信号"。消费者内部通过 `select` 的 `case <-ctx.Done()` 感知到信号后，停止从队列抢新消息，处理完手头的最后一条消息后安全退出。
2. **第二层：Web 服务（HTTP Server）**：调用 `srv.Shutdown(ctx)` 关闭监听端口（不再接受新请求），并等待所有已建立的 HTTP 连接处理完毕后再退出。传入 `context.WithTimeout` 设定强制截止时间，防止死锁请求导致进程永远无法退出。

### 2.2 为什么需要两个独立的 Context？
- **`mainCtx` (WithCancel)**：控制自定义业务逻辑（MQ 消费者）。手动调用 `cancel()` 触发，无自动超时。
- **`shutdownCtx` (WithTimeout)**：控制第三方标准库（http.Server）。自带 5 秒强制超时，防止异常请求导致的无限阻塞。
- 两者是**两棵独立的 Context 树**，互不干扰。这样可以精确控制"先关消费者、再关 Web 服务"的**撤退时序**。

### 2.3 Go 并发核心概念
- **Goroutine 生命周期**：所有 Goroutine（包括 `main`）地位平等，但 `main` 协程退出时整个进程销毁，所有后台协程被强制终止。
- **Channel 阻塞机制**：从空的、无人写入的 Channel 读取数据会永久阻塞当前协程。`<-quit` 和 `<-ctx.Done()` 都是利用这一特性实现"等待信号"。
- **`select` 多路复用**：与 `switch` 不同，`select` 专门用于同时监听多个 Channel。当多个 Channel 同时就绪时，`select` 会**随机**选择一个执行，保证公平性。
- **Context 是显式传递的**：它不是全局变量，而是由开发者主动创建、主动通过函数参数逐层传递的"信号工具"。这种设计让开发者对每个后台任务都拥有精确的控制权。

## 3. 多级缓存架构 (L1 本地缓存 + L2 Redis)

在针对读多写少的极高频访问接口（例如秒杀系统的商品列表展示）中，仅仅使用 Redis 作为缓存也是不够的，必须引入本地进程的内存缓存构建多层防御：

### 3.1 既然 Redis 撑得住极高 QPS，为什么还要 Go 的本地缓存？
1. **规避网络 I/O 瓶颈**：尽管 Redis 本身的响应极快，但在巨大的 QPS 下，应用服务器和 Redis 之间的 TCP 网络传输会成为短板。"商品列表"这类结构体本身携带几十 KB 甚至更大容量的数据，如果十几万并发涌入，应用服务器本身的网卡带宽瞬间就会被打爆。
2. **抗击真正的"热点 Key" (Hotkeys)**：在极度狂热的抢购下，可能上百万用户在刷新同一个商品。借由本地缓存存活的区区 30 秒，便能将本应打向中心化 Redis 集群的一百万次请求，直接拦截压缩成 **1次** 的透传。剩下的几十万个请求甚至连网卡数据包都不会构造。

### 3.2 实现细节要点
1. **缓存打包机制 (原子性保证)**：当我们要缓存分页数据及其总记录数（Total）时，不能拆分成独立的两个 Key 分开写缓存，而是将其打包成一个独立的结构体（如 `ProductListCache`）。因为过期时间的毫秒落差一旦存在，就会导致客户端拿到了"新的商品列表"但是配的是"旧的商品总数"的逻辑错误。
2. **降级时长管理**：本地缓存的时效必须"极大短于"Redis 的时效（如30秒及5分钟）。保证绝佳的数据本地新鲜度的同时，在失效过期后依然能把 Redis 作为强力兜底盘，绝不暴露出最底层的核心 MySQL。

