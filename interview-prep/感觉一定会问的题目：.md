感觉一定会问的题目：
1、redis为什么快？Redis的数据结构底层原理？Redis是单线程吗？哪些是多线程？I/O多路复用是什么？Redisson是啥？

```
Redis 快主要有几个原因。
第一，它是内存数据库，大部分读写都在内存中完成，避免了磁盘随机 I/O。
第二，Redis 的命令执行主流程是单线程的，避免了多线程锁竞争和频繁上下文切换，而且单条命令执行时间通常很短。
第三，它采用 I/O 多路复用，一个线程可以同时监听大量客户端连接，连接有读写事件时再处理，所以网络模型比较高效。
第四，Redis 的底层数据结构做了很多优化，比如 String 使用 SDS，List 使用 quicklist，Hash/Set/ZSet 会根据数据规模在紧凑结构和哈希表、跳表之间切换，既节省内存又保证常见操作性能。
```
```
Redis的数据结构底层原理是什么？
Redis 顶层 key 空间是 dict，也就是哈希表。不同 value 类型有不同底层编码。String 是 SDS；List 现在主要是 quicklist；Hash 小数据量用 listpack，大了转 hashtable；Set 小整数集合用 intset，大了转 hashtable；ZSet 小数据量用 listpack，大了用 skiplist 加 dict。这样做的核心是根据数据规模在节省内存和查询效率之间做平衡。
```
```
Redis都是单线程吗？哪些是多线程？
严格说 Redis 不是所有东西都是单线程。我们通常说的单线程，指的是核心命令执行流程主要由主线程串行处理。后台持久化、异步删除、AOF 刷盘、Redis 6.0 后的网络 I/O 读写（在6.0之前是和命令共同作为主线程的），都可能用到其他线程或子进程。但命令执行本身串行，保证了很多操作天然原子，也避免了复杂锁竞争。
```
```
I/O多路复用是什么？
I/O 多路复用就是用一个线程（redis的I/O线程不是命令线程，网络I/O线程是多线程的）同时监听多个网络连接（客户端和redis建立的，是tcp连接，在redis初始化时设置了PoolSize=50 相当于连接池上限为50）。比如 Linux 下常见是 epoll，Redis 把客户端 socket 注册到事件循环里，当某个连接可读或可写时，事件循环才回调处理。这样不需要为每个连接创建一个线程，减少线程切换和内存开销，所以可以支撑大量连接（tcp）。

I/O复用的具体模型是什么？
Redis 底层会根据操作系统选择不同的 I/O 多路复用机制。在 Linux 上主要使用 epoll，在一些其他系统上可能使用 kqueue、select 等。
select 的特点是每次需要扫描一批文件描述符，连接多了以后开销会比较大，而且有连接数限制。epoll 更适合大量连接场景，它把要监听的 socket 注册到内核，事件就绪时内核通知应用程序，不需要每次遍历所有连接，所以在高并发连接下效率更高。

tcp连接动态扩张策略是什么？（是客户端管理的、go-redis）
是否有空闲可以用？没有空闲是否还能扩张？不能扩张只能等待空闲

I/O多路复用：
综合回答：Redis 基于事件循环处理网络请求。它不会一个客户端连接开一个线程，而是把多个 socket 注册到 I/O 多路复用器里。哪个 socket 有可读或可写事件，就被事件循环取出来处理。Linux 下 Redis 通常使用 epoll；其他系统可能使用 kqueue 或 select。
select 的问题是连接多时需要遍历大量 fd，开销比较高；epoll 是把 fd 注册到内核，等事件就绪后由内核通知应用，所以更适合大量连接但不是每个连接都一直活跃的场景。
放到我的项目里，秒杀时多个 Go 后端连接会同时向 Redis 发 Lua 请求，Redis 可以用 epoll 同时监听这些连接，发现哪个连接有请求就读取命令，然后交给主线程串行执行 Lua。这样网络连接管理是高效的，同时命令串行执行又保证了库存扣减这类操作的原子性。
```
```
Redisson是啥？
Redisson 是 Java 生态里一个比较常用的 Redis 客户端，它不只是简单封装 GET/SET，还基于 Redis 封装了很多分布式对象和工具，比如分布式锁、可重入锁、读写锁、延迟队列、限流器、布隆过滤器等。我项目是 Go 写的，没有直接用 Redisson，但类似分布式锁能力可以用 Redis 的 SET key value NX EX 加 Lua 解锁来实现。
Go 生态里有 redsync、redislock 这类 Redis 分布式锁库，但不像 Java 的 Redisson 那样提供完整分布式对象封装。我项目里没有用可重入锁，而是用 SET NX EX + UUID value + Lua 解锁 实现普通排他分布式锁。因为下单场景只需要限制同一用户同一时间只能进入一次下单流程，没有同一线程或同一请求重复进入同一把锁的需求，所以不可重入锁更简单，也更容易验证正确性。

Redis Cluster上的热点key怎么处理？
这个我项目里没有实际做 Redis Cluster 热点治理，但我理解热点 key 主要分读热点和写热点。读热点可以通过本地缓存、多级缓存、热点 key 副本、读写分离来缓解；写热点比较难，因为同一个 key 的写必须保证一致性，通常要做限流、削峰、拆 key 或者业务上降低单 key 写频率。我的项目里更接近的是用本地缓存和 IP 限流降低对 Redis 的压力，但没有做到完整 Cluster 热点治理。

关于微服务：
这块我没有实践过，所以不展开乱讲。我目前项目是单体加中间件部署，主要关注 Redis、MQ、MySQL 的链路可靠性。如果做多实例服务发现，我理解至少需要注册中心、健康检查、负载均衡和故障摘除，具体 Consul/Eureka 的实现细节我还需要补。
```

2、为什么用rabbitmq？不是还有各种kafka、RocketMQ吗？ 面试时不要答rabbitmq好因为他不好，而是结合业务场景去回答。你的mq用到了什么兜底策略，比如持久性怎么保证的（只有一个持久化参数设置？）？、

```
我项目里用 RabbitMQ，主要是为了做下单链路的异步削峰。普通下单和秒杀下单入口先在 Redis 里用 Lua 做库存校验和预扣，成功后把订单消息投递到 RabbitMQ，再由后台 consumer 按固定 worker 数和 prefetch 去做 MySQL 事务落库。这样可以避免秒杀瞬时流量直接打到 MySQL，也能让数据库写入压力变得可控。
选 RabbitMQ 是结合项目场景考虑的。我的场景是典型的业务队列：订单异步处理、手动 ack、失败重试、控制消费速度。RabbitMQ 的队列模型、ack 机制、prefetch、死信队列和 publisher confirm 都比较直接适配。
Kafka 更适合高吞吐日志流、埋点、数据管道、可回放消费这类场景，它的核心是 topic-partition 的日志模型。我的项目不是日志采集，也不需要特别大的流式吞吐，所以 Kafka 不是最优先。
RocketMQ 也很适合电商业务消息，支持事务消息、顺序消息、延迟消息等，但部署和使用复杂度相对更高。作为个人项目，我更需要一个轻量、容易落地、能覆盖订单异步削峰的 MQ，所以选择 RabbitMQ。
可靠性上，我做了几层兜底。第一，队列声明为 durable，消息设置 DeliveryMode = Persistent，保证 broker 重启后消息更不容易丢。第二，发布端开启 publisher confirm，生产者发完消息后等待 RabbitMQ broker 确认；如果确认失败或超时，就认为订单排队失败，并回滚 Redis 预扣库存。第三，consumer 关闭 auto ack，只有 MySQL 事务处理成功后才手动 ack；如果处理失败，会记录重试次数，未超过上限就进入重试队列，超过上限就进入死信队列。第四，因为 RabbitMQ 是至少一次投递语义，可能重复消费，所以 consumer 会用订单 ID 做幂等校验，订单 ID 也是数据库主键，重复消息不会重复落库。
所以我现在不是只设置了一个持久化参数，而是做了 durable queue、persistent message、publisher confirm、manual ack、重试/死信队列、Redis 回滚和订单 ID 幂等。不过这仍然不是完整分布式事务，生产上还可以继续补本地消息表、失败消息管理后台和告警。

消费者和broker的关系：
每个消费者通过consume在broker注册身份 msgs, err := ch.Consume(...) 其中msgs是<-chan amqp.Delivery 类型
broker按照prefetch数目给消费者push消息到msgs中，consumer是select监听msgs的

```

3、怎么保证多级缓存的一致性？你系统中的多级缓存是怎么设计的？redis里面都存了什么？go-cache呢？

**RabbitMQ 的 at-least-once delivery：至少投递一次，不保证只投递一次。**

RabbitMQ的TTL主要用于ready的消息，对于unacked的消息，需要自己实现timeout的逻辑。可以通过设置业务超时、prefetch、unacked 监控、consumer ack timeout 来处理。从3.8.15版本之后就增加了consumer-timeout的机制，也就是：
```
可以总结为三个条件，必须同时满足才会触发消费者超时：
连接正常：TCP 连接没有断开，心跳也正常。
存在未确认消息：该消费者（Channel）上，至少有一条消息处于 unacked 状态。
超时时间已到：这条 unacked 消息的存活时间，超过了设定的 consumer_timeout（默认 30 分钟）。
当这三个条件都满足时，RabbitMQ 会做两件事：
强制关闭通道：断开这个消费者的 Channel。

消息重新入队：把这条 unacked 消息（以及该 Channel 上其他 unacked 消息）重新放回队列，状态变回 ready，供其他消费者处理。
```



消费者和RabbitMQ之间通过AMQP协议通信：

``````
消费者程序
  ↓ 通过 AMQP 协议发送 basic.ack 帧
RabbitMQ broker
``````

```
redis（8种）和go-cache（1种）里面都存了什么数据？
go-cache：
1、product:list:page={page}:size={page_size}:cat={category_id}:kw={keyword}:sort={sort_by} 作为key，ProductListCache（Go 对象）作为value存储的，用于前端的商品信息展示，其中productlist和商品详情表记录列完全对应。但要注意的是，前端商品的库存信息来源是redis的stock:product_id。

redis：
1、product:list:page={page}:size={page_size}:cat={category_id}:kw={keyword}:sort={sort_by}，value类型是string，value是ProductListCache（GO 对象）序列化后的JSON，TTL设置为5min。获取内容后使用unmarshal解析即可得到数据。
2、snack:stock:{productID}类型为hash存储商品库存，用途包括：普通下单 Redis Lua 预扣库存，商品列表/详情返回前补库存，MQ 失败或业务失败时回滚库存，库存补偿任务校正。
3、seckill:stock:{activityID}秒杀库存，用于秒杀 Lua 判断和扣减活动库存。
4、seckill:user_count:{activityID}秒杀用户已购买计数，hash类型，field key按照user_id。
5、seckill:token:{activityID}:{userID} string类型，用于秒杀资格校验，value是HMAC token，TTL为60s，在秒杀下单时 Lua 中校验 token 并删除，保证一次性。
6、lock:order:user:{userID} / lock:seckill:{activityID}:user:{userID}  下单分布式锁 
限制同一用户并发重复下单/秒杀，怎么限制的？那就是用户获取令牌后执行秒杀或者普通下单的请求，需要先获得锁。保证了同一用户时间内只能完成一次下单/秒杀操作
7、order:idempotency:{userID}:{idempotencyKey} 普通下单幂等key。防止同一个请求重复提交。 注意这个idempotencykey是由前端随机生成的uuid，后端会执行SET order:idempotency:{userID}:abc processing NX EX 10min 所以也就是用分布式锁 防止用户同意请求重复提交，如果用户故意换一个 idempotencyKey，它会被认为是新请求。这个到底是防什么的？防的是网络抖动，同一个用户请求被重传、重试。
8、order:stock_rollback:{orderID} 库存回滚幂等 key，防止同一条失败消息重复回滚 Redis 库存。
```
```
到底怎么防止重复回滚？什么时候可能发生重复回滚？
在重复投递了不可重试错误的情况下会回滚。

那什么情况下发生重复投递？组件异常，网络波动可以恢复。
因为RabbitMQ 是至少一次投递，应该先了解RabbitMQ到底什么时候会重新投递消息：
1、首先这条消息已经被投递出去但是没有ack，处于unacked状态。
2、以下都会让mq重新投递发出去但是unacked的消息：consumer异常，amqp channel异常。注意网络波动导致的ack丢失rabbitmq是不会重投的，因为mq的语义是at-least-once delivery：至少投递一次，不保证只投递一次。况且RabbitMQ consumer 和 broker 通信通常用的是 AMQP 协议，跑在 TCP 长连接 上，严格意义上的“ack 悄悄丢失但双方都完全正常”基本不会长期成立。因为TCP本身会做：可靠传输|顺序保证|丢包重传|连接异常检测。注意TCP连接是有序的是指字节顺序有序，但是多条消息传递过去，那么多消费者，消费者自己又有preftech，谁先执行完，就不一定了，所以TCP的顺序保证不是RabbitMQ的顺序保证。
```
```
redis库存补偿的工作是consumer在发现不可重试错误时自己完成的，严格情况下可以使用lua脚本：先判断回滚幂等性，然后再进行回滚。
```
```
有一个疑问那就是redis和gocache你提到的这些数据使用什么数据类型存的？
商品列表用的string; 库存用的string; seckill:user_count:{activityID}用的hash，field是user_id;
```
```
多级缓存的设计：
我项目里的多级缓存主要是针对商品列表查询，不是订单和库存明细。设计上是 Go-Cache 本地缓存作为 L1，Redis 作为 L2，MySQL 作为最终数据源。
查询商品列表时，会根据 page、page_size、category_id、keyword、sort_by 这些查询条件生成 cache key。请求先查 Go-Cache，如果命中直接返回；没命中再查 Redis，Redis 命中后会反填 Go-Cache；如果 Redis 也没命中，就查 MySQL，然后把结果写入 Redis 和 Go-Cache。这个是典型的 Cache Aside 模式。
```
```
缓存一致性的保证：
在一致性上，我对不不同业务做了不同的一致性保障：
go-cache和redis中的商品列表缓存：
对于使用三级缓存的商品列表，我这个项目没有做强一致缓存协议，而是用主动失效 + 短 TTL 控制不一致窗口。在管理员进行修改库存信息后，go-cache和redis中都会主动置商品列表缓存失效。
redis中的库存：
库存这种高频变化的数据，同样使用的是主动更新+定时补偿+TTL被动失效保证一致性。管理端更改库存会主动更新，同时后台有定时补偿任务，保证redis库存不出现异常值，TTL作为保底机制。
redis中的其他数据如秒杀分布式锁、用户限购数目、秒杀库存、秒杀token：
秒杀token、分布式锁（lock:order:user:{userID}）、重复回滚幂等key、防止重复下单都是短ttl失效机制。
用户限购数目、秒杀库存这种除了设置ttl为秒杀活动剩余时间外，还有主动失效机制。
总结一句话：当前项目保证的是最终一致性，不是强一致性。单实例下主动失效 + 短 TTL 已经够用；多实例场景需要增加缓存失效广播机制，比如通过MQ广播、配置中心事件来增强。
```
4、SSO、JWT的优缺点？为什么使用JWT？不用redis+token？有cookie、session为啥用JWT？SSO可以结合校园的统一认证回答。

```
首先明确概念：
Cookie是浏览器保存数据的机制，不是认证方案本身，假如使用Session，那么session_id就可以存放在Cookie中，Cookie还可以放其他数据比如：用户语言偏好=zh-cn、主题设置=dark、购物车ID=xxx。
Session 是服务端保存登录态，浏览器通常只用存一个session_id在Cookie中。
Token 是一种登录凭证，可以放在 Header 或 Cookie 里。
JWT 是一种自包含 Token，里面可以带 user_id、过期时间、签名等信息。
（Token和JWT的区别就是Token是饮料，那JWT就是可乐）

为什么选用JWT？
我这个项目是前后端分离的校园电商系统，后端主要提供 REST API，所以认证上选择 JWT。用户登录后，后端签发 access token 和 refresh token。平时访问商品、订单、下单、秒杀、管理端接口时，前端只把 access token 放到请求头，后端中间件验签后解析出 user_id，不需要每次都查 Redis 或维护传统 Session，认证链路比较轻，也方便后续水平扩展。
但我没有把 JWT 做成一个长期 token，因为长期 access token 泄露后风险窗口太大。所以项目里采用双 token：access token 有效期短，只负责访问业务接口；refresh token 有效期长，只负责在 access token 过期后换新 token。refresh token 同时会存 Redis，退出登录、改密码或刷新时可以删除旧 refresh token，让它不能继续续期。
这样做的取舍是：业务接口大部分时间仍然是无状态 JWT 校验，不会每次请求都查 Redis；只有刷新 token、退出登录这类认证生命周期操作才访问 Redis。它比传统 Session 更轻，也比单个长期 JWT 更安全。针对JWT无状态服务端无法主动失效、进行权限变更等缺点，可以在JWT 基础上引入 Redis 黑名单或 token_version，而不是一开始就把所有登录态都放 Redis。

为啥不适用redis+token或者sessionID？
Redis + token 和 SessionID 都属于服务端状态方案，可控性强，但每次鉴权都需要查 Redis 或维护 Session 状态。我的项目里 Redis 已经在库存预扣、秒杀和商品缓存里承担高并发压力，所以普通业务接口鉴权不希望再强依赖 Redis。JWT access token 可以让大部分请求只做本地验签，减少 Redis 访问和服务端状态维护；再配合 Redis 中的 refresh token 做续期和退出失效，兼顾性能和可控性。

SSO是什么？SSO（Single Sign-On，单点登录）是一种身份认证方案，允许用户只需登录一次即可访问多个相互信任的应用系统。
```

5、怎么做的压测？
```
我的压测是用 Node.js 写的轻量级脚本完成的，没有上 k6/JMeter。主要原因是项目规模不大，我更关注核心链路稳定性和瓶颈定位，所以用 Node 内置 fetch 配合 Promise.all 模拟并发 worker，通过环境变量控制并发数、持续时间和 think time。
压测不是只压一个接口，我分了几个场景：首先 smoke 测试验证登录、商品、下单、订单查询链路是否跑通；然后 read baseline 压商品列表、商品详情、分类、秒杀列表，用来观察多级缓存下的读性能；接着 shopping mix 模拟真实用户浏览、下单、查订单的混合流量；再单独用 order_write 压普通下单链路，重点观察 Redis 预扣库存、RabbitMQ 削峰和消费者落库；最后用 seckill_spike 模拟秒杀突刺，验证秒杀 token、Redis Lua 扣库存、用户限购和 MQ 异步处理是否能防止超卖。
指标上我主要记录总请求数、QPS、失败率、状态码分布、P50/P90/P95/P99 延迟和错误信息。同时压测过程中会观察后端日志、Redis、MySQL 连接、RabbitMQ ready/unacked。如果下单压测结束后 MQ 能逐步清空、库存没有负数、Redis 和 MySQL 最终收敛，说明削峰和最终一致性链路基本可用
```
```
为什么用node.js，不用专业工具？
主要是为了快速、可控地贴合我的业务接口。k6/JMeter 更专业，但我这个阶段更需要把业务流程串起来，比如登录、取商品、下单、秒杀 token、执行秒杀，这些流程用 Node.js 脚本写起来很直接，也方便和项目一起维护。后续如果要做更正式的容量评估，可以迁移到 k6，并接入 Prometheus/Grafana 做更完整的监控分析。
```
```
脚本是我借助 AI 辅助生成的，但我自己按业务链路拆了场景，并确认了每个脚本压的是哪条链路、指标怎么统计、压完要检查哪些系统状态。AI 主要提高了脚本编写效率，压测设计和结果分析还是围绕我的项目架构来的。
```
6、你说的这个预热，具体是怎么做的？
```
我项目里预热分两类。普通商品库存是在服务启动时预热，启动时从 MySQL 查询所有商品，然后把每个商品的库存写入 Redis，key 是 snack:stock:{productID}。这样普通下单时可以直接在 Redis 中用 Lua 脚本做库存校验和扣减，避免每个请求都查 MySQL。
秒杀活动预热是管理端手动触发的。管理员创建活动后，在活动时间范围内调用预热接口，后端查询活动信息，把活动库存写入 seckill:stock:{activityID}，同时给秒杀库存和用户限购计数设置到活动结束时间的 TTL，并把活动状态更新为 active。这样秒杀开始后，请求会先在 Redis 层完成 token 校验、库存扣减和限购判断，成功后再进入 RabbitMQ 异步落库。
```
7、你说用lua脚本校验，我看你在回答中不止一次说可以用lua脚本，到底用了几次，都做了哪些任务？

```
一共有三个lua脚本：
1、关于下单链路：
两轮遍历请求的productlist，第一次校验库存，第二次循环进行DECRBY。


2、秒杀链路：
1. 校验购买数量是否合法（是否购买数量小于0或者为空？）
2. 校验 seckill token 是否存在且匹配
3. 删除 token，保证一次性使用
4. 检查秒杀库存是否足够
5. 检查用户是否超过限购
6. 扣减 seckill:stock:{activityID}
7. 增加 seckill:user_count:{activityID} 里的用户购买数
8. 刷新库存 key 和用户计数 key 的 TTL 到活动结束


3、分布式锁包括普通下单用户锁/秒杀用户锁
（lock:order:user:{userID} / lock:seckill:{activityID}:user:{userID}）：脚本名为releaseLua，释放分布式锁时，先判断锁 value 是否是自己持有的，再删除。为什么要用 Lua？
因为释放锁不能简单 DEL key。
```
8、你说你做了幂等校验，是干嘛的？为啥要做幂等校验？

```
幂等校验就是：同一个业务请求执行一次和执行多次，最终结果一样。
我项目里的幂等主要做在下单和 MQ 消费两个阶段。因为下单接口会先在 Redis 里预扣库存，然后把订单消息投递到 RabbitMQ，最后由消费者异步落库。这个链路里如果用户重复提交、MQ 重复投递、消费者重试，都可能导致重复扣库存、重复创建订单或者库存和订单不一致，所以我做了入口防重、消费端查重、数据库条件扣减和失败补偿。
在下单阶段，做了包括：下单后按钮置灰、下单请求在前端js代码生成idempotencyKey，用于防止网络波动重复提交订单。Redis Lua脚本会IdempotencyKey（也是Set NX虽然属于分布式锁类型但是一般不叫锁，单叫幂等key，因为幂等key是ttl到期后释放，不会lua主动释放，不存在竞争）+lock（下单锁，每个userID顶多一把，用完要释放）+库存/限购校验，校验成功则生成业务ID，发布消息，等待消费者校验业务ID，开启消费事务。
在消费阶段，订单以业务ID作为主键，消费者先用SQL语句查询DB确认订单不存在后，开启事务，进行库存、余额的校验。对于校验通过的事务，业务ID作为订单表主键，可以从DB层面兜底幂等性，不论消费者是否异常，DB主键的唯一性可以防止MQ重复投递后不会被消费者重复消费。对于不可重试错误，比如库存、余额不足，则在完成redis库存回滚的同时设置roll_back:业务ID的回滚幂等key（同样是分布式锁，但是不称为锁，因为不主动释放而是ttl到期后释放），防止回滚后，消费者异常，没来得及nack，mq重复投递，再次回滚库存。


为啥要做幂等，因为在分布式系统中很常见：
用户重复点击
接口超时重试 网络波动
MQ重复投递 因为RabbitMQ的语义是至少投递一次，不等于至少消费一次，为啥？因为每条消息RabbiitMQ投递后不设置超时，在消费者a连接断开后才会将发给消费者a的、处于nack的消息重新投递给消费者bcd。
消费者处理失败后重试 可能是数据库连接超时、连接池枯竭、db宕机等等 现在有做兜底吗？
Redis 扣了但 MQ/DB 没成功导致库存不一致

所以做幂等校验的目的是不是为了防止重复，而是重复发生时的处理。
```
8、超卖、少卖？
```
超卖：超卖的主要是指库存只有 1 件，但是并发来了 2 个请求，两个都扣成功了。
我们是 Redis 层先用 Lua 原子预扣，挡住高并发入口的超卖；MySQL 层再用 stock >= num 的条件更新做最终兜底,防止缓存和数据库不一致时把库存扣成负数；此外还做了redis库存单向定时补偿，若Redis > MySQL库存，则Redis 库存虚高，有超卖风险，定时任务检查后进行修改。
导致Redis库存>MySQL的情形：管理员修改库存/回滚操作幂等失效/Redis数据被误操作，脏写。

少卖：Redis 库存已经扣了，但订单没有最终成功，导致库存被占住，明明还能卖却卖不出去。
少卖主要靠回滚机制解决。因为我的下单是 Redis 先预扣、MQ 异步落库，所以只要后续 MQ 投递失败或者消费端发现订单不能成立，就必须把 Redis 预扣库存回补。为了防止重复回补导致超卖，我用 order:stock_rollback:{orderID} 做补偿幂等。最后还有定时任务做 Redis 和 MySQL 库存对账，修复异常库存缓存。
```
9、mq具体怎么用？你设置了哪些参数、哪些模式？（只有手动ack、和wokerNum/prefetch？）

```
1、publisher confirm：amqp消息发布后，等待mqBroker ack，ttl为5s，超时则redis回滚。
2、手动ack：取消auto ack。自己在消费者中处理ack，实现不可重试错误回滚和可重试错误的处理的工作。
3、durable
```



10、RPC和HTTP的区别？以及gRPC是啥？什么是websocket？
11、缓存击穿、穿透、雪崩怎么应对？（其实概念都没记清、、还有布隆过滤器怎么用的原理是什么？）
12、死信队列有了解吗？
13、你刚刚提到主从、集群，这些具体概念是什么？你是怎么保证主从redis服务器之间数据的一致性的？redis集群三大方案（主从/哨兵-高可用/集群）怎么选？用了集群后业务有什么变化？代码需要做什么处理？ 什么脑裂/raft/选举都是什么？ 主从复制方案下，怎么保证主redis读写时，系统仍能拿到真实数据？
14、分布式是啥意思？你的系统用到了分布式吗？

```
分布式是什么？
分布式就是指把一个大任务拆成多个小任务，跑在不同的进程/机器上，它们通过网络协作完成。
分布式部署≠分布式架构 当前系统是分布式架构的，但是还是单机部署的，不属于分布式部署。如果是分布式部署，后端服务1、2、3跑在上海服务器，redis和mysql可能跑在北京服务器上。那么就需要考虑Nginx负载均衡、redis请求切片、redis集群模式/redis主从+哨兵方案考量。
那你用的分布式架构是怎么设计的？
其实就是依赖docker或者说本身是用mysql、mq组件就是分布式架构了。不同组件之间都是通过网络访问的。
frontend (nginx:80)  ←→  backend (Go :8080)  ←→  mysql (MySQL 8.4 :3306)  ←→  redis (Redis 7.4 :6379)  ←→  rabbitmq (RabbitMQ :5672)

```

15、Redis和Redisson有什么区别？什么叫可重入式锁？db和redis的锁有什么区别？都是怎么使用的？

Redis原生锁和RLock的主要区别：

![image-20260603143526213](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260603143526213.png)

```
Redis 锁叫分布式锁，是因为锁存在 Redis 这个公共服务里，而是多个机器、多个进程、多个服务实例共同竞争同一把锁，所以是分布式锁。


可重入式锁（RLock = Redisson 提供的分布式锁接口，而Redisson默认都是可重入式锁）？可重入锁的作用是防止同一个持有者在嵌套调用、重复封装、递归调用时再次申请同一把锁而发生死锁。注意可重入式锁不是必须的，很多业务代码如果结构清晰，是不需要设置可重入式锁。
Redisson的RLock的特性：
基于 Redis
排他锁
可重入
支持自动续期 watchdog
支持 tryLock、lock、unlock
```
redis的锁和mysql的锁最主要的区别是：Redis锁与任何数据库事务无关，它是纯业务逻辑的协调工具。需要开发者手动调用lock()和unlock()，且必须处理异常情况（如业务中途崩溃，锁依靠超时释放）。
从代码上看：

![image-20260603144646235](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260603144646235.png)

![image-20260603144730157](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20260603144730157.png)

15、容灾处理：你说你用了redis、mq、db 这些宕机或者流量过大的时候有什么应对处理？ mq积压怎么处理？
16、订单30分钟自动取消是怎么做的？没付款自动取消怎么做的？我的系统完全没有涉及到啊，都没有未支付这个状态、、、（乐观锁，应该怎么改进？）
17、分库分表是什么意思？分库分表后业务有什么变化？
18、你的核心线程池是怎么设置的？线程数设置了多少？为什么？
19、你用了雪花算法，为什么用雪花算法而不是其他的？
20、你的golang的中间件用了几层？
21、数据库的acid的原理是什么？
22、你系统中订单的状态流转是怎么设计的？
23、下单链路：具体可看CreateOrder函数

```
先请求下单lock，然后校验idempotencyKey，然后lua库存校验，然后snowflake生成业务ID，然后publish消息。
```

24、innodb索引失效

25、排查慢查询

挺重要的问题：
1、channel相关的东西 woker pool的设置？channel怎么用的？协程的context是啥？
2、Redis和MemCached有啥区别？
3、mvcc底层机制、事务隔离级别、数据库的锁类型；其实什么undo、redo、bin log这些是啥我不清楚 ；
4、具体什么是回表？（有问过我，但是我感觉回答不够精炼，主要还是因为不完全了解、没背明白）；mysql和postgreSQL主要的区别在哪？
5、了解一下nginx
6、消息的顺序消费怎么实现？
7、mysql的innodb和myisam有啥区别？
8、不停机数据迁移、不停机发布怎么做？
9、慢sql优化有哪些思路？
原则一：能讲明白为什么选A，不选B、C。要对A的原理清楚，对于B、C了解、弄明白主要特点即可。



codex补充的问题：
1、异步下单返回成功时订单是否已经落库？前端怎么知道最终状态？
2、重复下单和重复消费有什么区别？分别怎么解决？
3、MQ 消费失败无限重试怎么办？哪些错误应该进死信队列？
4、RabbitMQ durable、persistent、publisher confirm 分别解决什么问题？
5、多实例部署时 Redis 库存预热、本地缓存失效、限流会有什么问题？
6、金额为什么不能用 float64？应该怎么改？
7、订单状态机怎么设计？取消、退款、失败重试分别怎么流转？

```
当前系统的订单状态包括四种状态：待支付、已支付、已完成、已取消
当前使用的是“下单占库存、支付扣余额”模型。好处是能防止用户支付时发现没货，代价是pending订单会短暂占住库存，需要做超时自动取消/用户主动取消的库存回滚逻辑。
```



8、生产环境为什么不能直接用 AutoMigrate？
9、JWT 角色变更、token 过期、secret 泄露分别怎么处理？
10、压测除了 QPS 还要看哪些指标？worker/prefetch 调大会有什么副作用？

**建议优先补充这些**

1. 异步下单返回后，订单还没落库，前端怎么展示订单状态？
   追问：轮询、WebSocket、订单状态机怎么设计？
2. 普通下单重复点击怎么防？
   你现在清单里有“幂等”，但要区分：
   - 重复下单：用户/前端重复发起不同请求
   - 重复消费：同一条 MQ 消息被重复处理
     建议补：idempotency_key、Redis SET NX EX、DB 唯一约束。
3. MQ 失败重试会不会无限重试？
   补充：
   - 哪些错误可重试？
   - 哪些错误不该重试，比如余额不足、商品下架？
   - 怎么设计重试次数、死信队列、失败表？
4. 你现在没有 publisher confirm，怎么保证消息真的投递成功？
   你已有“持久化参数”，但面试官可能会追：
   - durable queue 和 persistent message 分别保证什么？
   - publisher confirm 解决什么？
   - broker 收到但还没落盘时宕机会怎样？
5. Redis 预扣成功，MQ 投递成功，但 consumer 一直失败怎么办？
   补：
   - Redis 库存会不会一直少？
   - 如何补偿？
   - 本地消息表/订单状态表怎么设计？
6. 多实例部署时，库存预热会不会覆盖运行时库存？
   这个很容易被问。你项目启动时会预热普通商品库存，生产上要说：
   - 启动预热适合演示/开发
   - 生产要避免多实例重复覆盖
   - 秒杀预热应作为明确运营动作，带版本/状态控制
7. 本地缓存多实例怎么失效？
   你写了 go-cache/L1 缓存，但多实例下管理端清缓存只能清当前进程。补：
   - Redis pub/sub
   - MQ 广播
   - 短 TTL
   - 不一致窗口怎么接受
8. 管理端取消/退款和用户取消/退款逻辑是否一致？
   你的文档里已经指出：管理端订单状态更新可能只回补 MySQL，没有完整回补 Redis。这是一个很好的“主动暴露并改进”的问题。
9. 金额为什么不能用 float64？
   电商项目高频问。补：
   - float64 有精度问题
   - 应用层用 int64 分为单位，或 decimal 库
   - DB 用 decimal 只是存储层，不代表 Go 计算安全

1. GORM AutoMigrate 生产能不能用？
   补：
   - 开发环境方便
   - 生产用版本化 migration
   - 回滚、灰度、字段兼容怎么处理

**Go 方向建议补**

1. Gin 中间件执行顺序是什么？你的 RequestID、限流、Prometheus、Auth 谁先谁后？
2. context.Context 在项目里怎么用？
   如果没怎么用，也要知道怎么改：请求取消、超时控制、DB/Redis 调用传递 context。
3. goroutine 泄漏怎么排查？
   结合 consumer worker、定时补偿任务、限流清理任务回答。
4. channel 关闭原则是什么？谁创建谁关闭？多个 goroutine 发送时怎么安全关闭？
5. Go map 并发读写为什么会 panic？你的 IP limiter 为什么要加锁？
6. defer、panic、recover 在 Web 服务里怎么用？Gin 是否需要全局恢复中间件？

**数据库方向建议补**

1. 为什么订单项要保存商品快照价？商品改价后历史订单怎么办？
2. 地址是否应该做订单快照？
   现在订单没有收货地址快照的话，用户修改地址会影响历史订单展示，这是业务设计追问。
3. 订单状态机怎么设计？
   待支付、待处理、已完成、已取消、退款中、已退款之间哪些流转合法？
4. 唯一索引怎么设计？
   比如：
   - 用户名唯一
   - 手机号唯一
   - 秒杀 (user_id, activity_id) 唯一
   - 幂等 key 唯一
5. 分页深翻页怎么优化？
   limit offset 大页码慢，怎么用游标分页或基于 ID 翻页？

**安全方向建议补**

1. JWT secret 泄露怎么办？
2. 用户角色变更后，旧 token 是否立即失效？
3. 管理员接口怎么做审计日志？
4. 密码为什么用 bcrypt？和 MD5/SHA256 有什么区别？
5. CORS 怎么配置？生产环境能不能 *？

**压测和监控建议补**

1. 压测结果怎么证明有效？
   不只说 QPS，要说 RT、TP95/TP99、错误率、Redis/MQ/MySQL 指标。
2. RabbitMQ 的 ready、unacked、ack rate 分别代表什么？
3. worker/prefetch 调大有什么副作用？
   DB 压力、连接池耗尽、事务冲突、失败重试放大。
4. Prometheus 指标怎么设计告警？
   比如错误率、P99、MQ 积压、consumer 失败率、Redis 延迟。



























# **早上好，开始学习吧任重道远啊！！！！！**
